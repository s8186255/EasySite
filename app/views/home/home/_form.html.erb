<%= form_for(@home_home) do |f| %>
  <% if @home_home.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home_home.errors.count, "error") %> prohibited this home_home from being saved:</h2>

      <ul>
      <% @home_home.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
